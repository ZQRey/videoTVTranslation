<?xml version="1.0" encoding="utf-8"?>
<ScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/scrollView"
    android:layout_width="520dp"
    android:layout_height="wrap_content"
    android:background="@drawable/bg_card_dark"
    android:overScrollMode="never"
    android:fillViewport="true"
    android:focusable="false"
    android:descendantFocusability="afterDescendants"
    android:padding="28dp">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:focusable="false"
        android:descendantFocusability="afterDescendants"
        android:orientation="vertical">

        <!-- Шапка диалога -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:gravity="center_vertical"
            android:orientation="horizontal">

            <ImageView
                android:layout_width="32dp"
                android:layout_height="32dp"
                android:contentDescription="@null"
                android:src="@drawable/ic_tv"
                app:tint="@color/brand_emerald" />

            <LinearLayout
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginStart="12dp"
                android:orientation="vertical">

                <TextView
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:fontFamily="sans-serif-bold"
                    android:text="@string/settings_title"
                    android:textColor="@color/text_primary"
                    android:textSize="18sp" />

                <TextView
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:text="@string/settings_subtitle"
                    android:textColor="@color/text_secondary"
                    android:textSize="12sp" />
            </LinearLayout>
        </LinearLayout>

        <View
            android:layout_width="match_parent"
            android:layout_height="1dp"
            android:layout_marginTop="16dp"
            android:layout_marginBottom="16dp"
            android:background="@color/bg_card_stroke" />

        <!-- 1. Поле ввода хоста сервера -->
        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:fontFamily="sans-serif-medium"
            android:text="@string/server_host_label"
            android:textColor="@color/text_primary"
            android:textSize="13sp" />

        <EditText
            android:id="@+id/etServerHost"
            android:layout_width="match_parent"
            android:layout_height="48dp"
            android:layout_marginTop="6dp"
            android:background="@drawable/bg_input_focused"
            android:focusable="true"
            android:focusableInTouchMode="true"
            android:nextFocusUp="@id/etServerHost"
            android:nextFocusDown="@id/rbRtsp"
            android:fontFamily="monospace"
            android:hint="@string/server_host_hint"
            android:imeOptions="actionNext"
            android:inputType="textUri|textNoSuggestions"
            android:paddingStart="14dp"
            android:paddingEnd="14dp"
            android:singleLine="true"
            android:textColor="@color/text_primary"
            android:textColorHint="@color/text_muted"
            android:textSize="13sp" />

        <!-- 2. Выбор протокола (RTSP / HLS) -->
        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="16dp"
            android:fontFamily="sans-serif-medium"
            android:text="@string/stream_protocol_label"
            android:textColor="@color/text_primary"
            android:textSize="13sp" />

        <RadioGroup
            android:id="@+id/rgProtocol"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="6dp"
            android:focusable="false"
            android:orientation="vertical">

            <RadioButton
                android:id="@+id/rbRtsp"
                android:layout_width="match_parent"
                android:layout_height="44dp"
                android:layout_marginBottom="6dp"
                android:background="@drawable/bg_radio_focused"
                android:buttonTint="@color/brand_emerald"
                android:checked="true"
                android:focusable="true"
                android:nextFocusUp="@id/etServerHost"
                android:nextFocusDown="@id/rbHls"
                android:paddingStart="8dp"
                android:paddingEnd="8dp"
                android:text="@string/protocol_rtsp"
                android:textColor="@color/text_primary"
                android:textSize="13sp" />

            <RadioButton
                android:id="@+id/rbHls"
                android:layout_width="match_parent"
                android:layout_height="44dp"
                android:background="@drawable/bg_radio_focused"
                android:buttonTint="@color/brand_emerald"
                android:focusable="true"
                android:nextFocusUp="@id/rbRtsp"
                android:nextFocusDown="@id/etPort"
                android:paddingStart="8dp"
                android:paddingEnd="8dp"
                android:text="@string/protocol_hls"
                android:textColor="@color/text_primary"
                android:textSize="13sp" />
        </RadioGroup>

        <!-- 3. Порты и путь потока в строке -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="14dp"
            android:baselineAligned="false"
            android:focusable="false"
            android:orientation="horizontal">

            <LinearLayout
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_marginEnd="8dp"
                android:layout_weight="1"
                android:focusable="false"
                android:orientation="vertical">

                <TextView
                    android:id="@+id/tvPortLabel"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:fontFamily="sans-serif-medium"
                    android:text="@string/rtsp_port_label"
                    android:textColor="@color/text_primary"
                    android:textSize="12sp" />

                <EditText
                    android:id="@+id/etPort"
                    android:layout_width="match_parent"
                    android:layout_height="44dp"
                    android:layout_marginTop="4dp"
                    android:background="@drawable/bg_input_focused"
                    android:focusable="true"
                    android:focusableInTouchMode="true"
                    android:nextFocusUp="@id/rbHls"
                    android:nextFocusRight="@id/etStreamPath"
                    android:nextFocusDown="@id/rbSchedGlobal"
                    android:fontFamily="monospace"
                    android:inputType="number"
                    android:paddingStart="12dp"
                    android:paddingEnd="12dp"
                    android:singleLine="true"
                    android:text="8554"
                    android:textColor="@color/text_primary"
                    android:textSize="13sp" />
            </LinearLayout>

            <LinearLayout
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_marginStart="8dp"
                android:layout_weight="1"
                android:focusable="false"
                android:orientation="vertical">

                <TextView
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:fontFamily="sans-serif-medium"
                    android:text="@string/stream_path_label"
                    android:textColor="@color/text_primary"
                    android:textSize="12sp" />

                <EditText
                    android:id="@+id/etStreamPath"
                    android:layout_width="match_parent"
                    android:layout_height="44dp"
                    android:layout_marginTop="4dp"
                    android:background="@drawable/bg_input_focused"
                    android:focusable="true"
                    android:focusableInTouchMode="true"
                    android:nextFocusUp="@id/rbHls"
                    android:nextFocusLeft="@id/etPort"
                    android:nextFocusDown="@id/rbSchedGlobal"
                    android:fontFamily="monospace"
                    android:inputType="textNoSuggestions"
                    android:paddingStart="12dp"
                    android:paddingEnd="12dp"
                    android:singleLine="true"
                    android:text="live"
                    android:textColor="@color/text_primary"
                    android:textSize="13sp" />
            </LinearLayout>
        </LinearLayout>

        <!-- 4. Расписание вещания клиента -->
        <View
            android:layout_width="match_parent"
            android:layout_height="1dp"
            android:layout_marginTop="16dp"
            android:layout_marginBottom="14dp"
            android:background="@color/bg_card_stroke" />

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:fontFamily="sans-serif-medium"
            android:text="@string/schedule_title"
            android:textColor="@color/text_primary"
            android:textSize="13sp" />

        <RadioGroup
            android:id="@+id/rgScheduleMode"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="6dp"
            android:focusable="false"
            android:orientation="vertical">

            <RadioButton
                android:id="@+id/rbSchedGlobal"
                android:layout_width="match_parent"
                android:layout_height="40dp"
                android:layout_marginBottom="4dp"
                android:background="@drawable/bg_radio_focused"
                android:buttonTint="@color/brand_emerald"
                android:checked="true"
                android:focusable="true"
                android:nextFocusUp="@id/etPort"
                android:nextFocusDown="@id/rbSched247"
                android:paddingStart="8dp"
                android:paddingEnd="8dp"
                android:text="@string/schedule_mode_global"
                android:textColor="@color/text_primary"
                android:textSize="12sp" />

            <RadioButton
                android:id="@+id/rbSched247"
                android:layout_width="match_parent"
                android:layout_height="40dp"
                android:layout_marginBottom="4dp"
                android:background="@drawable/bg_radio_focused"
                android:buttonTint="@color/brand_emerald"
                android:focusable="true"
                android:nextFocusUp="@id/rbSchedGlobal"
                android:nextFocusDown="@id/rbSchedInterval"
                android:paddingStart="8dp"
                android:paddingEnd="8dp"
                android:text="@string/schedule_mode_24_7"
                android:textColor="@color/text_primary"
                android:textSize="12sp" />

            <RadioButton
                android:id="@+id/rbSchedInterval"
                android:layout_width="match_parent"
                android:layout_height="40dp"
                android:background="@drawable/bg_radio_focused"
                android:buttonTint="@color/brand_emerald"
                android:focusable="true"
                android:nextFocusUp="@id/rbSched247"
                android:nextFocusDown="@id/btnCancel"
                android:paddingStart="8dp"
                android:paddingEnd="8dp"
                android:text="@string/schedule_mode_interval"
                android:textColor="@color/text_primary"
                android:textSize="12sp" />
        </RadioGroup>

        <LinearLayout
            android:id="@+id/layoutScheduleInterval"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="8dp"
            android:baselineAligned="false"
            android:focusable="false"
            android:orientation="horizontal"
            android:visibility="gone">

            <LinearLayout
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_marginEnd="8dp"
                android:layout_weight="1"
                android:focusable="false"
                android:orientation="vertical">

                <TextView
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:fontFamily="sans-serif-medium"
                    android:text="@string/schedule_start_label"
                    android:textColor="@color/text_primary"
                    android:textSize="12sp" />

                <EditText
                    android:id="@+id/etScheduleStart"
                    android:layout_width="match_parent"
                    android:layout_height="44dp"
                    android:layout_marginTop="4dp"
                    android:background="@drawable/bg_input_focused"
                    android:focusable="true"
                    android:focusableInTouchMode="true"
                    android:nextFocusUp="@id/rbSchedInterval"
                    android:nextFocusRight="@id/etScheduleEnd"
                    android:nextFocusDown="@id/btnCancel"
                    android:fontFamily="monospace"
                    android:hint="08:00"
                    android:inputType="time"
                    android:paddingStart="12dp"
                    android:paddingEnd="12dp"
                    android:singleLine="true"
                    android:text="08:00"
                    android:textColor="@color/text_primary"
                    android:textSize="13sp" />
            </LinearLayout>

            <LinearLayout
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_marginStart="8dp"
                android:layout_weight="1"
                android:focusable="false"
                android:orientation="vertical">

                <TextView
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:fontFamily="sans-serif-medium"
                    android:text="@string/schedule_end_label"
                    android:textColor="@color/text_primary"
                    android:textSize="12sp" />

                <EditText
                    android:id="@+id/etScheduleEnd"
                    android:layout_width="match_parent"
                    android:layout_height="44dp"
                    android:layout_marginTop="4dp"
                    android:background="@drawable/bg_input_focused"
                    android:focusable="true"
                    android:focusableInTouchMode="true"
                    android:nextFocusUp="@id/rbSchedInterval"
                    android:nextFocusLeft="@id/etScheduleStart"
                    android:nextFocusDown="@id/btnSaveConnect"
                    android:fontFamily="monospace"
                    android:hint="20:00"
                    android:inputType="time"
                    android:paddingStart="12dp"
                    android:paddingEnd="12dp"
                    android:singleLine="true"
                    android:text="20:00"
                    android:textColor="@color/text_primary"
                    android:textSize="13sp" />
            </LinearLayout>
        </LinearLayout>

        <!-- Подсказка результирующего URL -->
        <TextView
            android:id="@+id/tvResultPreview"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="12dp"
            android:background="@color/bg_input"
            android:fontFamily="monospace"
            android:padding="8dp"
            android:textColor="@color/text_secondary"
            android:textSize="11sp"
            tools:text="URL: rtsp://192.168.1.100:8554/live" />

        <!-- Кнопки действий (Подключиться / Отмена) -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="22dp"
            android:focusable="false"
            android:gravity="end"
            android:orientation="horizontal">

            <androidx.appcompat.widget.AppCompatButton
                android:id="@+id/btnCancel"
                android:layout_width="wrap_content"
                android:layout_height="46dp"
                android:layout_marginEnd="12dp"
                android:background="@drawable/bg_btn_secondary_focused"
                app:backgroundTint="@null"
                android:focusable="true"
                android:nextFocusLeft="@id/btnCancel"
                android:nextFocusRight="@id/btnSaveConnect"
                android:nextFocusUp="@id/rbSchedInterval"
                android:nextFocusDown="@id/btnCancel"
                android:fontFamily="sans-serif-medium"
                android:paddingStart="20dp"
                android:paddingEnd="20dp"
                android:text="@string/btn_cancel"
                android:textColor="@color/text_primary"
                android:textSize="13sp" />

            <androidx.appcompat.widget.AppCompatButton
                android:id="@+id/btnSaveConnect"
                android:layout_width="wrap_content"
                android:layout_height="46dp"
                android:background="@drawable/bg_btn_focused"
                app:backgroundTint="@null"
                android:focusable="true"
                android:nextFocusLeft="@id/btnCancel"
                android:nextFocusRight="@id/btnSaveConnect"
                android:nextFocusUp="@id/rbSchedInterval"
                android:nextFocusDown="@id/btnSaveConnect"
                android:fontFamily="sans-serif-medium"
                android:paddingStart="24dp"
                android:paddingEnd="24dp"
                android:text="@string/btn_connect"
                android:textColor="@color/text_primary"
                android:textSize="13sp" />
        </LinearLayout>

    </LinearLayout>
</ScrollView>
